import { Component } from '@angular/core';

@Component({
  selector: 'app-account',
  templateUrl: './account.component.html',
  styleUrls: ['./account.component.scss']
})
export class AccountComponent {

  // use statemanagement to check whether the user is signed in or not,
  // if the user is not signed in redirect to the login/register component
  // account page should load the user component and a maintenance complaint component 
  //________________________________________________________________________________//
  // Admin component should get its own dashboard for flipping between building info tabs



}
